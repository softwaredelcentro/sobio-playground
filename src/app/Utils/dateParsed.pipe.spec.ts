import { DateParsedPipe } from './dateParsed.pipe';

describe('DateParsedPipe', () => {
  it('create an instance', () => {
    const pipe = new DateParsedPipe();
    expect(pipe).toBeTruthy();
  });
});
